<!DOCTYPE html>
<html>
  <head>
    <title>{{TITLE}} - Blog</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{SUMMARY}}">

    <!-- OpenGraph metadata -->
    <meta property="og:title" content="{{TITLE}}">
    <meta property="og:description" content="{{SUMMARY}}">
    <meta property="og:type" content="{{OG_TYPE}}">
    <meta property="og:url" content="{{BLOG_URL}}">
    <meta property="og:site_name" content="Personal Blog">
    <meta property="article:published_time" content="{{PUBLISHED_DATE}}">
    <meta property="article:author" content="Joshua Hyde">{{OG_IMAGE_META}}

    <!-- Twitter Card metadata -->
    <meta name="twitter:card" content="{{TWITTER_CARD_TYPE}}">
    <meta name="twitter:title" content="{{TITLE}}">
    <meta name="twitter:description" content="{{SUMMARY}}">{{TWITTER_IMAGE_META}}

    <link rel="stylesheet" href="{{CSS_PATH}}">
  </head>
  <body>
    <div class="blog-navigation">
      <a href="{{HOME_PATH}}">← Home</a> | <a href="{{BLOG_INDEX_PATH}}">← Blog Index</a>
    </div>
    <article class="blog-post">
      {{TABLE_OF_CONTENTS}}
      {{CONTENT}}
    </article>
    <div class="blog-footer">
      <p>Found an issue with this post or have feedback? <a href="https://github.com/jrh3k5/personal-page/issues" target="_blank" rel="noopener noreferrer">Report an issue on GitHub</a></p>
    </div>
    <div class="blog-navigation">
      <a href="{{HOME_PATH}}">← Home</a> | <a href="{{BLOG_INDEX_PATH}}">← Blog Index</a>
    </div>
  </body>
</html>